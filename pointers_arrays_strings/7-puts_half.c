#include "main.h"

int length (char *s);

void puts_half(char *str)
{
	int i = 0;
	while (str[i] = '\0')
	{
		i++;
	}

}

int length (char *s)
{
	int b = 0;
	while (s[b] != '\0')
	{
		b++;
	}
	return (b);
}
